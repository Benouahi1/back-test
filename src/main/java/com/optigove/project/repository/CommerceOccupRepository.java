package com.optigove.project.repository;

import com.optigove.project.model.CommerceOccup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CommerceOccupRepository extends JpaRepository<CommerceOccup,Long> {
}
