package com.optigove.project.repository;

import com.optigove.project.model.TransportPublic;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransportPublicRepository extends JpaRepository<TransportPublic,Long> {
}
